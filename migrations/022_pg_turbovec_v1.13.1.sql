-- pg_turbovec v1.13.1 -- Phase C metadata-filtering docs + crossover.
--
-- This file is a *reference mirror*. The authoritative install
-- script is generated by `cargo pgrx schema`.
--
-- v1.13.1 is a DOCS + BENCH release. It adds docs/FILTERING.md (the
-- canonical guide to the three working metadata-filter mechanisms:
-- partial index, in-kernel allowlist turbovec.knn(..., allowed), and
-- iterative-scan + WHERE), measures the allowlist selectivity
-- crossover, and corrects stale competitive/parity docs. It also
-- fixes three pre-existing broken benchmark sources.
--
-- There is NO source-logic change, NO SQL surface change, and NO
-- wire-format change. MetaPageData::version stays 4. There is no
-- DDL, no on-disk migration, and NO REINDEX. `ALTER EXTENSION
-- pg_turbovec UPDATE TO '1.13.1';` resolves against this
-- intentionally-empty file.
