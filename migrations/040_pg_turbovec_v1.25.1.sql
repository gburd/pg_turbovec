-- pg_turbovec v1.25.1 -- release-tooling + docs/benchmark patch. NO
-- shippable code change: the compiled binary is byte-identical to
-- v1.25.0.
--
-- This file is a *reference mirror*. The authoritative install script
-- is generated by `cargo pgrx schema`.
--
-- NO wire-format change (stays VERSION 6), NO SQL-surface change (no
-- new/removed operators, types, functions, GUCs, or opclasses). NO
-- REINDEX.
--
-- What changed since v1.25.0 (none of it touches src/ runtime code):
--   * Added a tag-triggered PGXN + postgresql.org-news publish pipeline
--     (.forgejo/workflows/release.yml, scripts/make-dist.sh,
--     META.json.in, ci/announce.sh) -- this is the release that first
--     exercises that automation.
--   * Added the Qdrant + ANN-Benchmarks-protocol competitive benchmark
--     ( +
--     benches/results/qdrant_annbench_20260709/), which validated
--     v1.25.0's turbovec.hi_dim_rerank at scale (GIST-960-1M recall
--     0.876 -> 0.953) and produced the competitive-position numbers.
--   * A docs correction (SIFT R@0.99 latency: the 2026-07-08 doc's
--     16.8ms was the out-of-core path; in-RAM is 2.6ms).
--
-- Migration: `ALTER EXTENSION pg_turbovec UPDATE TO '1.25.1';` (a
-- no-op upgrade -- nothing in the SQL surface or on-disk format
-- changed). Purely a docs/tooling/bench patch release.
